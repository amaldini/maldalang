<Window x:Class="MaldaLang.DesktopIDE.Windows.ReplayToHereDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Replay to Here" Height="400" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,8" Foreground="{DynamicResource TextForegroundBrush}">
            Restore file system state up to the selected step. Choose where to write files.
        </TextBlock>
        <TextBlock Grid.Row="1" Text="Working directory:" Margin="0,8,0,4" Foreground="{DynamicResource TextForegroundBrush}"/>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="WorkingDirTextBox" Grid.Column="0" Margin="0,0,8,0" Padding="4"/>
            <Button Grid.Column="1" Content="Browse..." Click="BrowseButton_Click" Padding="8,4"/>
        </Grid>
        <TextBlock Grid.Row="3" Text="Files to restore:" Margin="0,12,0,4" Foreground="{DynamicResource TextForegroundBrush}"/>
        <ListBox x:Name="FilesListBox" Grid.Row="4" Margin="0,4,0,0"
                 BorderThickness="1" BorderBrush="{DynamicResource InputBorderBrush}"
                 Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource InputForegroundBrush}"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" IsCancel="True" Click="CancelButton_Click" Padding="12,4" Margin="0,0,8,0"/>
            <Button x:Name="RestoreButton" Content="Restore" IsDefault="True" Click="RestoreButton_Click" Padding="12,4"/>
        </StackPanel>
    </Grid>
</Window>
