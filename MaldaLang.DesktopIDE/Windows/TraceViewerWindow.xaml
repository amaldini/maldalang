<Window x:Class="MaldaLang.DesktopIDE.Windows.TraceViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Trace Viewer" Height="600" Width="1000"
        FontFamily="Segoe UI, Segoe UI Variable, sans-serif"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Padding="8,6" Background="{DynamicResource ToolbarBackgroundBrush}" BorderBrush="{DynamicResource ToolbarBorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Path Style="{StaticResource IdeIcon}" Data="{StaticResource IconTrace}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Go to step" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource TextSecondaryBrush}"/>
                <TextBox x:Name="GoToStepTextBox" Width="64" VerticalAlignment="Center" KeyDown="GoToStepTextBox_KeyDown" Padding="6,4"/>
                <Button x:Name="GoToStepButton" Style="{StaticResource ToolbarButton}" Click="GoToStepButton_Click" Margin="6,0,0,0" Content="Go"/>
                <Border Style="{StaticResource ToolbarDivider}"/>
                <Button x:Name="PrevStepButton" Style="{StaticResource ToolbarButton}" Click="PrevStepButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <Path Style="{StaticResource IdeIcon}" Data="{StaticResource IconChevronLeft}"/>
                        <TextBlock Text="Previous" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button x:Name="NextStepButton" Style="{StaticResource ToolbarButton}" Click="NextStepButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Next" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <Path Style="{StaticResource IdeIcon}" Data="{StaticResource IconChevron}"/>
                    </StackPanel>
                </Button>
                <Border Style="{StaticResource ToolbarDivider}"/>
                <Button x:Name="ReplayToHereButton" Style="{StaticResource ToolbarButton}" Click="ReplayToHereButton_Click" ToolTip="Restore file system state up to selected step">
                    <StackPanel Orientation="Horizontal">
                        <Path Style="{StaticResource IdeIcon}" Data="{StaticResource IconReload}"/>
                        <TextBlock Text="Replay to Here" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button x:Name="RunFromHereButton" Style="{StaticResource ToolbarButton}" Click="RunFromHereButton_Click" ToolTip="Start new agent execution from selected step">
                    <StackPanel Orientation="Horizontal">
                        <Path Style="{StaticResource IdeIcon}" Data="{StaticResource IconPlay}"/>
                        <TextBlock Text="Run from Here" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button x:Name="BranchFromHereButton" Style="{StaticResource ToolbarButton}" Click="BranchFromHereButton_Click" ToolTip="Create new trace branch from selected step">
                    <StackPanel Orientation="Horizontal">
                        <Path Style="{StaticResource IdeIcon}" Data="{StaticResource IconAdd}"/>
                        <TextBlock Text="Branch from Here" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="200"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="3*" MinWidth="250"/>
            </Grid.ColumnDefinitions>
            <!-- Left: Event list -->
            <DataGrid x:Name="EventsDataGrid" Grid.Column="0"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                      HeadersVisibility="Column" SelectionMode="Single" SelectionChanged="EventsDataGrid_SelectionChanged"
                      Background="{DynamicResource ListBackgroundBrush}" Foreground="{DynamicResource ListForegroundBrush}"
                      BorderBrush="{DynamicResource ListBorderBrush}" BorderThickness="0,0,1,0"
                      RowHeight="22" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{DynamicResource BorderBrush}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Step" Binding="{Binding StepIndex}" Width="50"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                    <DataGridTextColumn Header="Agent" Binding="{Binding AgentName}" Width="80"/>
                    <DataGridTextColumn Header="Summary" Binding="{Binding Summary}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="{DynamicResource GridSplitterBackgroundBrush}"/>
            <!-- Right: Details -->
            <TabControl Grid.Column="2" Background="{DynamicResource WindowBackgroundBrush}" BorderThickness="0">
                <TabItem Header="Details">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="8">
                        <TextBox x:Name="DetailsTextBox" IsReadOnly="True" TextWrapping="NoWrap" AcceptsReturn="True"
                                 FontFamily="Consolas" FontSize="12" VerticalScrollBarVisibility="Disabled"
                                 Background="Transparent" BorderThickness="0" Padding="0"/>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="File state" x:Name="FileStateTab">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock x:Name="FileStateLabel" Grid.Row="0" Margin="8,8,8,4" Text="Path, operation, and content for FileEdit events."
                                   TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="8">
                            <TextBox x:Name="FileStateTextBox" IsReadOnly="True" TextWrapping="NoWrap" AcceptsReturn="True"
                                     FontFamily="Consolas" FontSize="12" VerticalScrollBarVisibility="Disabled"
                                     Background="Transparent" BorderThickness="0" Padding="0"/>
                        </ScrollViewer>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>
