<Window x:Class="MaldaLang.DesktopIDE.Windows.TraceViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Trace Viewer" Height="600" Width="1000"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource InputForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Navigation bar: Go to step, Prev, Next -->
        <Border Grid.Row="0" Padding="8,4" Background="{DynamicResource ToolbarBackgroundBrush}" BorderBrush="{DynamicResource ToolbarBorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Go to step:" VerticalAlignment="Center" Margin="0,0,4,0" Foreground="{DynamicResource TextForegroundBrush}"/>
                <TextBox x:Name="GoToStepTextBox" Width="60" VerticalAlignment="Center" KeyDown="GoToStepTextBox_KeyDown"/>
                <Button x:Name="GoToStepButton" Content="Go" Click="GoToStepButton_Click" Margin="4,0,0,0"/>
                <Button x:Name="PrevStepButton" Content="◀ Previous" Click="PrevStepButton_Click" Margin="16,0,0,0"/>
                <Button x:Name="NextStepButton" Content="Next ▶" Click="NextStepButton_Click" Margin="4,0,0,0"/>
                <Button x:Name="ReplayToHereButton" Content="Replay to Here" Click="ReplayToHereButton_Click" Margin="16,0,0,0" ToolTip="Restore file system state up to selected step"/>
                <Button x:Name="RunFromHereButton" Content="Run from Here" Click="RunFromHereButton_Click" Margin="4,0,0,0" ToolTip="Start new agent execution from selected step"/>
                <Button x:Name="BranchFromHereButton" Content="Branch from Here" Click="BranchFromHereButton_Click" Margin="4,0,0,0" ToolTip="Create new trace branch from selected step"/>
            </StackPanel>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="200"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="3*" MinWidth="250"/>
            </Grid.ColumnDefinitions>
            <!-- Left: Event list -->
            <DataGrid x:Name="EventsDataGrid" Grid.Column="0"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                      HeadersVisibility="Column" SelectionMode="Single" SelectionChanged="EventsDataGrid_SelectionChanged"
                      Background="{DynamicResource ListBackgroundBrush}" Foreground="{DynamicResource ListForegroundBrush}"
                      BorderBrush="{DynamicResource ListBorderBrush}" BorderThickness="0,0,1,0"
                      RowHeight="22" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{DynamicResource BorderBrush}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Step" Binding="{Binding StepIndex}" Width="50"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                    <DataGridTextColumn Header="Agent" Binding="{Binding AgentName}" Width="80"/>
                    <DataGridTextColumn Header="Summary" Binding="{Binding Summary}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="{DynamicResource GridSplitterBackgroundBrush}"/>
            <!-- Right: Details -->
            <TabControl Grid.Column="2" Background="{DynamicResource WindowBackgroundBrush}" BorderThickness="0">
                <TabItem Header="Details">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="8">
                        <TextBox x:Name="DetailsTextBox" IsReadOnly="True" TextWrapping="NoWrap" AcceptsReturn="True"
                                 FontFamily="Consolas" FontSize="12" VerticalScrollBarVisibility="Disabled"
                                 Background="Transparent" BorderThickness="0" Padding="0"/>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="File state" x:Name="FileStateTab">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock x:Name="FileStateLabel" Grid.Row="0" Margin="8,8,8,4" Text="Path, operation, and content for FileEdit events."
                                   TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="8">
                            <TextBox x:Name="FileStateTextBox" IsReadOnly="True" TextWrapping="NoWrap" AcceptsReturn="True"
                                     FontFamily="Consolas" FontSize="12" VerticalScrollBarVisibility="Disabled"
                                     Background="Transparent" BorderThickness="0" Padding="0"/>
                        </ScrollViewer>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>
