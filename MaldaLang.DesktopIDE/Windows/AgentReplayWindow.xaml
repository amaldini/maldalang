<Window x:Class="MaldaLang.DesktopIDE.Windows.AgentReplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Run from Here - Agent Replay" Height="500" Width="700"
        FontFamily="Segoe UI, Segoe UI Variable, sans-serif"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Path Style="{StaticResource IdeIcon}" Data="{StaticResource IconThink}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock TextWrapping="Wrap" VerticalAlignment="Center" Foreground="{DynamicResource TextForegroundBrush}">
                Conversation restored from trace. Enter a prompt and click Think to continue from this point.
            </TextBlock>
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource DialogPanel}" Margin="0,0,0,12">
            <ScrollViewer x:Name="MessagesScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="10">
                <TextBlock x:Name="MessagesTextBlock" TextWrapping="Wrap" Foreground="{DynamicResource ListForegroundBrush}" FontFamily="Consolas" FontSize="12"/>
            </ScrollViewer>
        </Border>
        <TextBox x:Name="PromptTextBox" Grid.Row="2" MinHeight="64" Margin="0,0,0,12"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="ThinkButton" Click="ThinkButton_Click" IsDefault="True" Style="{StaticResource PrimaryButton}" MinWidth="100">
                <StackPanel Orientation="Horizontal">
                    <Path Style="{StaticResource IdeAccentIcon}" Data="{StaticResource IconThink}" Stroke="{DynamicResource PrimaryButtonForegroundBrush}"/>
                    <TextBlock Text="Think" Margin="6,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>
