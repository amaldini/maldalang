# MALDA 0.1.52

Public announcement copy refreshed for post–P0 / Spec Final reality.

## Highlights

- **Announcement** — [`docs/announcement.md`](../announcement.md) updated from stale
  0.1.31 / Draft claims: toolchain **0.1.52**, Spec **Final 1.0**, IDE type Errors by
  default, honest workflow deny-list wording, micro-benchmark samples, workspace packages /
  selective import / `export type`.

## Requirements

Same as 0.1.0:

- .NET 8 SDK
- Windows for the Desktop IDE; CLI / Web IDE / LSP are intended to work wherever .NET 8 does

## Install without sources

Download `malda-0.1.52-win-x64.zip` (CLI + Desktop IDE) or `malda-0.1.52-linux-x64.zip`
(CLI only) from the GitHub Release assets, unzip, and run:

```bash
# Windows
malda.bat Examples\Basics\hello_world.malda
MaldaDesktop.bat

# Update an existing win-x64 install in place:
powershell -File scripts\update-local-win-x64-release.ps1 -Force

# Linux
./malda Examples/Basics/hello_world.malda
```

## Quick start (from source)

```bash
git clone https://github.com/amaldini/maldalang.git
cd maldalang
git checkout v0.1.52
dotnet build MaldaLang
dotnet run --project MaldaLang -- Examples/Basics/hello_world.malda
```

## Notes

- Still **0.1.x** toolchain numbering. Language Spec is **Final 1.0**.
- See also [`docs/releases/v0.1.51.md`](v0.1.51.md).
